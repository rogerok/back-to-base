# Structure of repo

`1.folder-name` - where 1 is number of lesson
In every lesson's folder there are folders divided by theme.

In each theme folder there are two folders:

- `docs` with info about theme
- `practice` with practical tasks.